import { View, Text } from 'react-native';

export default function OngsScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center', backgroundColor: '#FEFDF9' }}>
      <Text style={{ color: '#718096' }}>Tela de ONGs</Text>
    </View>
  );
}